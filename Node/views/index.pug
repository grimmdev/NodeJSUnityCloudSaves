extends layout

block content
  div(class='container-fluid')
      div(class='row')
          h2 Saves
          div(class='table-responsive')
            table(class='table table-striped table-sm')
              thead
                tr
                    th(scope='col') #
                    th(scope='col') ID
                    th(scope='col') Data
              tbody